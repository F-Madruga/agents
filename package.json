{
  "name": "agents",
  "version": "0.1.0",
  "description": "Personal agent skills + AGENTS.md, with a zero-dependency setup CLI",
  "type": "module",
  "private": true,
  "bin": {
    "agents": "cli/index.mjs"
  },
  "scripts": {
    "test": "node --test",
    "agent-setup": "npx -y github:F-Madruga/agents#agents-project --scope=project --store=.agents-store --all-skills --agents-md --agents=claude,cursor --force"
  },
  "engines": {
    "node": ">=18"
  }
}
